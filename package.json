{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "product-chain-authenticity",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/product-chain-authenticity-backend",
    "src/product-chain-authenticity-frontend"
  ],
  "dependencies": {
    "cargo": "^0.8.0",
    "hashcode": "^1.0.3",
    "uuid": "^9.0.1"
  }
}
